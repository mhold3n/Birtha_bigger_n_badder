This directory stores Homarr configuration generated by the container at runtime.
It is mounted into the Homarr container at `/app/data/configs`.

You can safely edit or back up files here. If empty, Homarr will start with a clean configuration and let you configure through the UI.

